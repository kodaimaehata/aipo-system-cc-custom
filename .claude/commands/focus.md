---
description: Decompose the current layer's goal into tasks (AIPO Focus).
---

# /focus

**Usage**: `/focus [LayerPath]`

Loads the **AIPO Focus Skill** to breakdown the goal.

## Inputs
- Optional: layer path (recommended when multiple layers exist), e.g. `programs/<project_name>` or `programs/<project_name>/sublayers/<sublayer_name>`

## Execution
1. Read `.claude/skills/aipo-core/SKILL.md` and follow its rules.
2. Read `.claude/skills/aipo-focus/SKILL.md` and execute its instructions.
