---
description: Generate execution commands for the current layer (AIPO Discover).
---

# /discover

**Usage**: `/discover [LayerPath]`

Loads the **AIPO Discover Skill** to generate execution plans.

## Inputs
- Optional: layer path (recommended when multiple layers exist), e.g. `programs/<project_name>` or `programs/<project_name>/sublayers/<sublayer_name>`

## Execution
1. Read `.claude/skills/aipo-core/SKILL.md` and follow its rules.
2. Read `.claude/skills/aipo-discover/SKILL.md` and execute its instructions.
